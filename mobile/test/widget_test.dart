import 'package:flutter_test/flutter_test.dart';

void main() {
  // Integration tests will be added in Phase 2 once Firebase and server
  // dependencies can be mocked. Placeholder keeps the test suite green.
  testWidgets('placeholder', (tester) async {
    expect(1 + 1, 2);
  });
}
